[project]
name = "gs-pino"
version = "0.2.0"
description = "U-FNO/PINO surrogate for fixed and free-boundary Grad-Shafranov equilibria"
requires-python = ">=3.10"
dependencies = [
  "numpy>=1.23",
  "torch>=2.0",
  "matplotlib>=3.6",
  "tqdm>=4.64",
]

[tool.setuptools.packages.find]
where = ["src"]

[project.scripts]
gs-pino-generate = "gs_pino.generate_dataset:main"
gs-pino-train = "gs_pino.train:main"
gs-pino-evaluate = "gs_pino.evaluate:main"
gs-pino-generate-freegs = "gs_pino.generate_freegs_dataset:main"
gs-pino-train-freegs = "gs_pino.train_freegs:main"
